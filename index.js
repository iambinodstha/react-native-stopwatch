/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import StopWatch from './source/StopWatch';

AppRegistry.registerComponent(appName, () => StopWatch);
